FROM flink:1.7.1-scala_2.11-alpine

# Add Prometheus jar
COPY ./prometheus/flink-metrics-prometheus_2.11-1.7.1.jar /opt/flink/lib

# Update entrypoint
COPY docker-entrypoint.sh /
RUN chmod +x /docker-entrypoint.sh /